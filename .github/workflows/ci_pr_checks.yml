name: PR Checks

on:
  pull_request:
    branches: [ main, 'release/v*' ]

permissions:
  contents: read
  packages: read
  checks: write

jobs:
  test:
    name: Test and Validate
    uses: ./.github/workflows/test.yml
    permissions:
      contents: read
